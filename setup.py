from setuptools import setup

setup(
    name="MultiChromeTester",
    version="1.0",
    description="Ferramenta para testar sites em múltiplas instâncias do Chrome",
    author="Seu Nome",
    packages=[""],
    install_requires=[
        "selenium",
        "webdriver-manager",
    ],
)